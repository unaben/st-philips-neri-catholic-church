export type WhoItsFor = {
  icon: string;
  title: string;
  description: string;
};

export type JourneyStage = {
  stage: string;
  title: string;
  timing: string;
  description: string;
};

export type FaqItem = {
  question: string;
  answer: string;
};
