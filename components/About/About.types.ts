export type TimelineEvent = {
  year: string;
  title: string;
  description: string;
};

export type ValueCard = {
  icon: string;
  title: string;
  description: string;
};

export type FactItem = {
  icon: string;
  label: string;
  value: string;
};
